<%# can use this for breadcrumb idea... right now used in application.html do display game name at top.. %>
<% content_for(:html_title) { @game.name} %>

<%# we can edit what this looks like in the _game.html.erb %>

<%= render partial: @game, locals: {show_link: false} %>


